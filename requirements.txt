# python-telegram-bot с поддержкой SOCKS прокси
# extras [socks] добавляет httpx-socks для SOCKS5/SOCKS4 протоколов
python-telegram-bot[socks]==22.5

# httpx[socks] — чтобы httpx (который под капотом у ptb) понимал socks5://
httpx[socks]==0.28.1

# Хранение секретов
python-dotenv==1.1.1

# Транзитивные зависимости — фиксируем для воспроизводимости сборки
anyio==4.11.0
certifi==2025.8.3
h11==0.16.0
httpcore==1.0.9
idna==3.10
sniffio==1.3.1
typing_extensions==4.15.0
